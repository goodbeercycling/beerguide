import React from 'react';

test('renders learn react link', () => {
    // render(<AppHeader
    //     selected="Thursday"
    // />);
    // const teamTitleHeaderElement = screen.getByText("TGB bar guide");
    // expect(teamTitleHeaderElement).toBeInTheDocument();
});
